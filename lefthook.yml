pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,jsx,tsx,json}"
      run: npm run biome -- {staged_files}
      staged_files: true
    prettier:
      glob: "*.{css,scss}"
      run: npm run prettier -- {staged_files}
      staged_files: true